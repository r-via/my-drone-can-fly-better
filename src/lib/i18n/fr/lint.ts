// Chaînes du lint de config CLI (src/lib/cli/config.ts) — référence FR.

export const lint = {
  // TODO(agent core-i18n) : extraire toutes les chaînes utilisateur de
  // src/lib/cli/config.ts (titles, details, evidence templates, fix).
} as const;
