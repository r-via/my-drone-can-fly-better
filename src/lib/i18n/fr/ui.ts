// Chaînes de l'interface (layout, page, composants, graphes) — référence FR.

export const ui = {
  // TODO(agent ui-i18n) : extraire toutes les chaînes de src/app/* et
  // src/components/** (y compris les libellés des graphes SVG).
} as const;
