// Chaînes du moteur de règles (engine.ts) et des profils drones — référence FR.
// Rempli par extraction depuis engine.ts/profiles.ts ; les autres langues
// doivent refléter exactement cette structure (le type Dict l'impose).

export const rules = {
  // TODO(agent rules-i18n) : extraire toutes les chaînes utilisateur de
  // src/lib/rules/engine.ts et profiles.ts (titles, details, evidence
  // templates, fix, labels et notes de profils). Les entrées interpolées
  // sont des fonctions fléchées typées.
} as const;
